<h1>ツイート一覧</h1>
<%= link_to "新規登録", new_tweet_path %>

<ul>
  <% @tweets.each do |t| %>
    <li>
      <%= t.message %>
      <%= t.user_id %>
      <%= link_to "削除", tweet_path(t), data: { turbo_method: :delete} %>
    </li>
  <% end %>
</ul>

<%= link_to "ログアウト",  top_logout_path %>

